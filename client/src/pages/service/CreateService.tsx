import { ServiceForm } from "../../features/service";

const CreateService = () => {
    return (
        <ServiceForm title="Service creation" formType="create" />
    )
}

export default CreateService