const testValue = "test advertisement"; // to make create and delete easy process
const documentsCount = 1000; // the amount for creating advertisement documents

const seedAdsValues = {
    testValue,
    documentsCount
}

export default seedAdsValues;